services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - sqlite_data:/app/data
    environment:
      - DEBUG=True
      - ALLOWED_HOSTS=localhost,127.0.0.1
    restart: unless-stopped

volumes:
  sqlite_data:
